spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  datasource:
    username: postgres
    password: ${PASSWORD:291197}
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/authuser}